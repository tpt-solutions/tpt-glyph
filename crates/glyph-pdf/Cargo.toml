[package]
name = "glyph-pdf"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
authors = ["TPT Solutions"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/tpt-solutions/tpt-glyph"
homepage = "https://github.com/tpt-solutions/tpt-glyph"
description = "PDF parsing and rendering for TPT Glyph, built on the pdf crate and the shared glyph-core pipeline."

[dependencies]
glyph-core = { workspace = true }
pdf = "0.9"
thiserror = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
rayon = { workspace = true }
